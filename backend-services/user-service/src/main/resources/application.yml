server:
  port: 8081
spring:
  application:
    name: user-service-rw
  cloud:
    nacos:
      discovery:
        server-addr: 54.219.180.170:8848
        # ip: 54.115.103.18 # 改为user-service所在服务器的公网IP
        # port: 8081
        # 添加以下配置确保注册正确 IP
        ip: 127.0.0.1
        port: 8081
  datasource:
    url: jdbc:mysql://54.219.180.170:3306/ai_qa_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ai_qa_system
    # another DB
    # url: jdbc:mysql://9.78.190.130:3306/ai_qa_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    # username: remote_user_group2
    # password: Group2Password123!
    hikari:
      maximum-pool-size: 5 # 减少连接池大小
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true

# Swagger 配置 (正确的位置，与 spring 平级)
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# 日志配置
logging:
  file:
    name: application.log
    path: logs
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  charset:
    console: UTF-8
    file: UTF-8

jwt:
  secret: HhyD5jnwwGbyecOrbkAY6OEWNM/zDgF2qwFlF4MFAHznR4AaLweF8Xu71MF4TaYUfX7dRY9JIzOgF/yTYqx5Qg== # 生产环境建议使用复杂密钥并通过环境变量注入
  expiration: 86400000 # 24小时（单位：毫秒）
